export default defineAppConfig({
    ui: {
        primary: "orange",
        container: {
            base: "dark:bg-cool-900 mx-auto",
        },
        input: {
            base: "relative block w-50 disabled:cursor-not-allowed disabled:opacity-75 focus:outline-none border-0",
        },
    },
});
